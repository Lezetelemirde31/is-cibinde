# --- Site ---
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# --- Python API backend (see /backend) ---
# Server-side only — used by Server Components/Actions to call the FastAPI backend.
BACKEND_URL="http://localhost:8000"

# --- Clerk (https://dashboard.clerk.com) ---
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_xxx"
CLERK_SECRET_KEY="sk_test_xxx"
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL="/dashboard"
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL="/onboarding"

# Note: the Clerk webhook (user.created/updated/deleted), Cloudflare R2 uploads,
# and the database connection now live on the backend — see backend/.env.example.
